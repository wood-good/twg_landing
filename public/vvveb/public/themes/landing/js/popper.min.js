/* Popper.js v2 - Required by Bootstrap 5
 * Bootstrap 5 bundle already includes Popper, so this is a stub.
 * If you need standalone Popper, download from: https://popper.js.org/
 */

// Stub to prevent 404 errors
if (typeof Popper === 'undefined') {
  console.log('Popper.js: Using Bootstrap bundle which includes Popper');
}
